[project]
name = "scheduler"
version = "0.1.0"
description = "Application qui planifie des tâches. Elle communique avec une base de donnée et des api externes."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]
