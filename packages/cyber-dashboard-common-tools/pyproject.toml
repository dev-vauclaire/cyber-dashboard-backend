[project]
name = "cyber-dashboard-common-tools"
version = "0.1.0"
description = "Services techniques transverses comme le chiffrement"
readme = "README.md"
authors = [
    { name = "Dev", email = "dev@ch-vauclaire.fr" }
]
requires-python = ">=3.12"
dependencies = [
    "cryptography>=49.0.0",
]

[build-system]
requires = ["uv_build>=0.10.11,<0.11.0"]
build-backend = "uv_build"
